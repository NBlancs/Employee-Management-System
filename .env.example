# Login uses dummy auth (see app/utils/dummyAuth.ts). No backend URL required.

# Optional — only if you connect server /api/* proxy to a real backend again:
# NUXT_PUBLIC_API_BASE_URL=http://localhost:5000
